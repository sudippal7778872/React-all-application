route have two property 
render and component
<route path="" component={}>
<route path="" render={}>

we can create property and pass as props
let see in example.


render do only the changes
where component do the whole components